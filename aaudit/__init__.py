"""actions-audit: static supply-chain and reliability auditing for GitHub Actions.

Public entry points live in :mod:`aaudit.auditor` (rules and driver) and
:mod:`aaudit.miniyaml` (YAML loading with a PyYAML backend and a native
fallback).  The command line lives in ``actions_audit.py`` at the project root.
"""

__version__ = "1.0.0"
