"""Test package for actions-audit.

This empty package marker lets `python3 -m unittest discover -s tests` treat the
directory as importable, which is what makes the documented test command work
from the project root without setting PYTHONPATH.
"""
